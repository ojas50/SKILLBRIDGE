# Bug Tracker.md — Unified Bug Table

| BUG-ID | Status | Area | Problem | Next Action |
|--------|--------|------|---------|-------------|
| BUG-001 | ✅ Fixed | SkillBridge / Frontend | Incomplete node_modules transitivity on fresh scaffold | Ran npm install to resolve dependencies |
| BUG-002 | ✅ Fixed | SkillBridge / Backend | Static dummy data without database persistence | Implemented full SQLite schema with WAL mode & seed records |
| BUG-003 | ✅ Fixed | SkillBridge / Frontend | Plain layout without interactive simulators or dark glassmorphic styling | Built complete suite of 6 pages with dynamic capacity simulator and AI auditor |
| BUG-004 | ✅ Fixed | SkillBridge / Frontend | Redundant .babelrc conflicting with Next.js SWC font loader during production build | Removed .babelrc, enabled SWC compiler, validated full build passing |
| BUG-005 | ✅ Fixed | SkillBridge / Frontend & Backend | Lingering Hackathon and Problem Statement #26134 references in UI & metadata | Purged all occurrences across navbar, footer, metadata, hero, challenge sections and redeployed |
| BUG-006 | ✅ Fixed | SkillBridge / Frontend & Backend | Maharashtra skill development & state policy document mentions | Replaced with universal Directorate of Vocational Training / Technical Standards terminology and redeployed to Vercel |
| BUG-007 | ✅ Fixed | SkillBridge / Dashboard | Dashboard displaying static figures rather than personalized user parameters | Implemented interactive Dashboard Precision Calibration Suite with region, industry sector, cohort scale, and focus skills inputs; redeployed live |
| BUG-008 | ✅ Fixed | SkillBridge / Frontend | JSX unescaped entities in DataSourcesModal and missing Next.js Link in CurriculumSimulator | Fixed imports, escaped JSX entities, and verified full production build passing (15 static routes) |
| BUG-009 | ✅ Fixed | SkillBridge / Frontend | Browser autofill shows white background on input fields, breaking dark theme | Added CSS overrides for -webkit-autofill to force dark background (#0b1120) and light text |
| BUG-010 | ✅ Fixed | SkillBridge / Frontend | Navbar nav links hidden below 1280px (xl breakpoint), Sign In not discoverable | Changed nav breakpoint from xl to lg (1024px), improved mobile menu visibility |
| BUG-011 | ✅ Fixed | SkillBridge / Frontend (career-readiness) | `persistProfile` (page.tsx:165) copies the stored `readinessHistory` back unchanged — the freshly computed readiness % is never appended, so My Dashboard "Readiness Over Time" (gated on >1 entries) and the last-score readout can never populate | Appended `{date, percent}` of today's score into `readinessHistory` on save (replaces today's entry on repeat saves, not duplicates); moved callback after the readiness memo; My Dashboard chart + last-score now populate |
| BUG-012 | ✅ Fixed | SkillBridge / Frontend (career-readiness) | `JSON.parse(localStorage.getItem(profileKey) || "{}")` at page.tsx:165 is NOT try/catch-wrapped while the identical loader (line 148) is — malformed stored JSON throws and crashes the page on Save click | Wrapped parse in try/catch and shape-validated the parsed `readinessHistory` with `Array.isArray`; falls back to empty history on any malformed data |
| BUG-013 | ✅ Fixed | SkillBridge / Frontend (skill-gaps + CurriculumSimulator) | The Simulator never receives the skill the user clicked in the radar/table — parent only passes `onSimulateClick` which switches the tab; `defaultSkillId` prop is declared but never passed, so the simulator always starts at the first skill | Parent now stores `simulateSkillId` and passes it as `defaultSkillId`; SkillGapBar's `onSimulateClick(item.id)` fires the same handler (tab switch + skill set). Simulator mounts on each radar→simulator switch, so the state initializer picks up the clicked skill |
| BUG-014 | ✅ Fixed | SkillBridge / Frontend (CurriculumSimulator) | `selectedDistrict` lever (Pune Hub/etc.) is read in no calculation — visibly changes but affects zero outputs; `courseDurationMonths` is dead state with no UI control. UI presents 9 "policy parameters" but only 7 actually change the model | Both levers now live: per-corridor `DISTRICT_FACTORS` (seat efficiency + placement delta) feed the model, and Program Duration got a real UI control (6/9/12 months) that adds supply-boost and a 12-month placement penalty — all 9 levers change outputs |
| BUG-015 | ✅ Fixed | SkillBridge / Frontend (CurriculumSimulator) | Simulator computes gaps from STATIC `SKILL_INTELLIGENCE_DATA` while the skill-gaps page radar uses live `/api/skill-gaps` — the two gap numbers can diverge, confusing users | Parent now passes its live `skills` state through the typed `skillGaps` prop; simulator falls back to the static catalog only when the prop is empty |
| BUG-016 | ✅ Fixed | SkillBridge / Frontend (courses) | CourseModal status mapping (page.tsx:299) only maps 3 legacy states — "EMERGING / HIGH PRIORITY" courses are shown as "Oversupplied" in the Inspect modal | Mapped all 4 decisionStatus values (Emerging → "Emerging") and added an "Emerging" purple badge branch in CourseModal.tsx so the Inspect modal shows the correct status + color |
| BUG-017 | ✅ Fixed | SkillBridge / Frontend (courses) | Search predicate calls `c.missingSkills.some(...)` unguarded though the field is optional per type (guarded elsewhere with `&&`) — typing in search can throw if a course lacks `missingSkills` | Search now uses `(c.activeSkills ?? []).some(...)` / `(c.missingSkills ?? []).some(...)` — safe against malformed API rows |
| BUG-018 | ✅ Fixed | SkillBridge / Frontend (login/register/forgot-password) | `router.push()` invoked inside render (`if (user) { router.push(); return null; }`, a Next 14 anti-pattern); auth calls have no try/catch so a rejected promise leaves the button stuck on loading with no error shown | Redirect moved into `useEffect` on /login and /register (render keeps a `return null` guard, no side effect in render); login/register/google/forgot awaits wrapped in try/catch/finally — rejection now surfaces an inline error and resets the loading state |
| BUG-019 | ✅ Fixed | SkillBridge / Frontend (AuthContext) | `getStoredUsers()` `JSON.parse` result is never validated as array — `.find` crashes on malformed/legacy data; `hashPassword` is Java-style String hash (weak, prototype-grade); `forgotPassword` reveals account existence and never actually sends a reset | `getStoredUsers()` now verifies `Array.isArray` + per-user shape (`name`/`email`/`passwordHash` strings) → `[]` on bad data; restored-session `JSON.parse` shape-checked (invalid sessions dropped); `forgotPassword` always returns success (no enumeration; existing "If an account exists…" copy covers UX). Hash stays demo-grade — documented as such in a comment, upgrade to a real KDF is tracked for the auth rewrite |
| BUG-020 | ✅ Fixed | SkillBridge / Frontend (district-plans) | `navigator.clipboard.writeText` on copy has no catch — unhandled promise rejection on non-secure (http/tunnel) contexts | Wrapped in try/catch with a hidden-textarea `execCommand("copy")` fallback; "Copied" state only shown when the copy actually succeeded |
| BUG-021 | ✅ Fixed | SkillBridge / Frontend (light theme contrast, systemic) | 300/400-level accent text (emerald-300/400, blue-300, amber-300/400, rose-300, purple-300, cyan-300/400) is near-invisible on light glass cards (e.g. courses alignment-score `text-amber-400`, dashboard metrics); dozens of dark-glass chips (`bg-rose-950/40`, `bg-amber-950/30`, `bg-blue-950/20-80`, `bg-emerald-950/30`…) stay dark in light mode — same family as the AreaChart dark-in-light bug | One globals.css block scoped to `html[data-theme="light"]`: accent 300/400/500 (incl. `fit-*`) text remapped to 700-level shades; `bg-*-950/900` (+ hover) chips overridden to light tinted panels via attr selectors; `.badge-*` labels darkened on their tinted chips; `.text-fit-gradient` switched to a darker palette; always-dark navbar/footer re-assert bright accents with `!important`. Zero JSX edits — all 472+ touchpoints handled in CSS |
| BUG-022 | ✅ Fixed | SkillBridge / Frontend (employers) | `handleEmployerAdded` ids = `employers.length + 1` → duplicate ids/React keys possible once the list changes (API fetch or second add); handler parameter is untyped `any` | id now computed via functional update as `max(existing ids)+1` (no stale-closure dupes); modal payload typed via exported `NewEmployerInput` |
| BUG-023 | ✅ Fixed | SkillBridge / Frontend (my-dashboard) | "Readiness Over Time" chart hard-gated on `history.length > 1`, impossible while BUG-011 persists; also low-contrast accent text in light mode | Gate lowered to `length > 0` so the chart appears after the first Save (thanks to BUG-011 appends). Low-contrast half is subsumed by BUG-021 (light-theme contrast) → deferred to makeover Phase 1 |
| BUG-024 | ✅ Fixed | SkillBridge / Frontend (dashboard AreaChart) | Cockpit AreaChart grid lines & supply-area gradient stayed near-black in light mode (hardcoded slate/sky indigo stroke values) | Grid strokes + gradient stops now use `--chart-grid`, `--chart-supply`, `--chart-supply-soft` CSS vars (dark + light values in globals.css) — fixed this session, build passes |

**Status values:** Open | In Progress | ✅ Fixed | Wontfix

---

## Notes

- One row per bug.
- "Area" = layer (e.g. "SkillBridge / frontend", "SkillBridge / backend").
- After fixing, set Status to ✅ Fixed and update Taskflow.md.
